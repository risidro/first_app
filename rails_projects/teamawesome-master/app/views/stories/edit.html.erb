<h1><%= @story.title %></h1>
<h2><%= @story.category %></h2>

<center> <div>
  <% if @story.microposts.any? %>
    <%= render 'shared/feed' %>

  <% end %>
</div> </center>
<br/>

<% 
=begin %>
This is just some garbage I was trying when I was overcomplicating.
	It's a nested attributes form. Leaving it in for a little while longer incase I want it later.
<% f.fields_for :microposts do |builder| %>  
<p>  
    <%= builder.label :content, "Question" %><br />  
    <%= builder.text_area :content, :rows => 3 %>  
  </p>  
<% end %>  
  <p><%= f.submit "Submit" %></p>  
<% 
=end %>


<section>
  <%= render 'shared/micropost_form' %>
</section>


<%= link_to 'Show', @story %> |
<%= link_to 'Back', stories_path %>
